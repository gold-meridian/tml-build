<Project>

    <PropertyGroup>
        <TmlBuildVersion>2.0.0</TmlBuildVersion>

        <Version>$(TmlBuildVersion)</Version>
        <PackageVersion>$(TmlBuildVersion)</PackageVersion>
    </PropertyGroup>

</Project>