<Project>

    <PropertyGroup>
        <TmlBuildVersion>2.1.0</TmlBuildVersion>

        <Version>$(TmlBuildVersion)</Version>
        <PackageVersion>$(TmlBuildVersion)</PackageVersion>
    </PropertyGroup>

</Project>