<Project>

    <PropertyGroup>
        <TmlBuildVersion>1.0.9</TmlBuildVersion>

        <Version>$(TmlBuildVersion)</Version>
        <PackageVersion>$(TmlBuildVersion)</PackageVersion>
    </PropertyGroup>

</Project>