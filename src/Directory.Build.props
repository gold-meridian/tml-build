<Project>

    <PropertyGroup>
        <TmlBuildVersion>2.1.1</TmlBuildVersion>

        <Version>$(TmlBuildVersion)</Version>
        <PackageVersion>$(TmlBuildVersion)</PackageVersion>
    </PropertyGroup>

</Project>